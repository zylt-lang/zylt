(
	$i			-- "import.dat (32 33 72 87 100 101 108 111 114)"
	$~ 2			-- "table[0]: 2 Setting"
	$[ $[ $> $] $] $. 	-- "Advance table twice and print"
	$+ $.			-- "Increment and output"
	$>			-- "Go to next table"
	$? 9			-- "Compare with table[0] and if it matches, jump to the 9th programme"
	$@ 17			-- "jump to the 17th programme"
	$_ $_			-- "Do nothing"
	$% 0			-- "jump to the table[0]"
	$.			-- "Print"
)
